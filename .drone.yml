---
name: default
type: docker
kind: pipeline


steps:
  - name: test
    image: python:3.8
    commands:
      - pip install pipenv
      - pipenv install --dev -v
      - pipenv run pytest
      - pipenv run mypy jsd_mp
